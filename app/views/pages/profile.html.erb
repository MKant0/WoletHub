<div class="row">
  <div class="container">
    <h2>Hola! <%= current_user.name %></h2>
    <div class="d-flex justify-content-center">
      <h3>Favoritos</h3>
    </div>
    <% @favorites.each do |favorite| %>
      <div class="baige-rounded-box-scaled align-center mx-auto">
        <%= favorite.recipient_account.holder_name %>
      </div>
    <% end %>


      <div class="form-actions py-5 mx-5">
        <div class="row">
          <div class="col-6 mt-2">
            <%= link_to "Editar perfil", edit_user_registration_path, class:"btn btn-purple-EditarPerfil", data:{turbo_frame: "modal_edit_user"} %>
          </div>
          <div class="col-6 text-end">
            <%= button_to 'Cerrar sesión', destroy_user_session_path, method: :delete, class: "btn btn-outline-primary purple-btn" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<%= turbo_frame_tag "modal_edit_user" %>
