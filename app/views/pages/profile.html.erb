<div class="row">
  <div class="container col-6 my-5 container-custom-outline-profile">
    <div class="text-left mx-5">
      <h3 class="personalInfo pt-5 text-center"><%= image_tag "identification.png", class: "profile-image" %>Informacion personal</h3>
      <div class="d-flex justify-content-between align-items-center">
        <h6 class="personalInfo pt-5">Nombre</h6>
        <h6 class="personalInfoindiv pt-5"><%= current_user.name %></h6>
      </div>
      <div class="d-flex justify-content-between align-items-center">
        <h6 class="personalInfo pt-5">Banco</h6>
        <h6 class="personalInfoBanks pt-5"><%= @bank_accounts.map(&:name).join(", ") %></h6>
      </div>
      <div class="d-flex justify-content-between align-items-center">
        <h6 class="personalInfo pt-5">Moneda de preferencia</h6>
        <h6 class="personalInfoCurrency pt-5"><%= current_user.preferred_currency %></h6>
      </div>
    </div>

    <!-- <% @favorites.each do |favorite| %>
      <div class="baige-rounded-box-scaled align-center mx-auto">
        <%= favorite.recipient_account.holder_name %>
      </div>
    <% end %> -->

      <div class="form-actions py-5 mx-5">
        <div class="row">
          <div class="col-6 mt-2">
            <%= link_to "Editar perfil", edit_user_registration_path, class:"btn btn-purple-EditarPerfil", data:{turbo_frame: "modal_edit_user"} %>
          </div>
          <div class="col-6 text-end">
            <%= button_to 'Cerrar sesión', destroy_user_session_path, method: :delete, class: "btn btn-outline-primary purple-btn" %>
          </div>
        </div>
      </div>
    </div>
  </div>


  <%= turbo_frame_tag "modal_edit_user" %>

</div>
