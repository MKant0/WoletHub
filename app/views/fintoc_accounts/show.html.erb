<div class="container my-4 puntitos-sin-100vh">
  <div class="d-flex flex-column justify-content-center align-items-center">

  <div class="container-custom-outline-profile col-10 d-flex justify-content-center m-5 bg-white">
    <div class="ring">
      <h2 class="ring-text">$<%= number_with_delimiter(@fintoc_account.balance.available, delimiter: ".", separator: ",") %></h2>
    </div>
  </div>

    <%# <div class="ring">
      <h2 class="ring-text">$<%#= number_with_delimiter(@fintoc_access[0].balance.to_s.split(" ")[0], delimiter: ".", separator: ",") %></h2>
    <%# </div> %>


    <%# <h2><%#= @fintoc_access[0].balance.to_s.split(" ")[0]</h2> %>
    <%#= console %>

    <div class="container-custom-outline-profile col-10 d-flex flex-column justify-content-center m-5 align-items-center p-3 bg-white">
      <div class="d-flex justify-content-center align-items-center ">
        <h3>Todas tus transacciones de la <%= @fintoc_account.name %> de <%= @bank_account.name %> </h3>
      </div>

      <div class="col-8 table-responsive small my-4">
        <table class="table table-striped table-sm">
          <thead>
            <tr>
              <th scope="col">Fecha</th>
              <th scope="col">Descripción</th>
              <th scope="col">Monto</th>
              <th scope="col">Moneda</th>
              <th scope="col">Destinatario</th>
            </tr>
          </thead>
          <tbody>
            <% @movements.each do |movement| %>
            <%#= @fintoc_movement.each do |movement| %>
            <%# <tr>  %>
              <%# if movement.sender_account %>
                <%# <td><%#= movement.sender_account.holder_name</td> %>
              <%# else %>
                <%# <td>No holder name available</td> %>
              <%# end %>
              <%# <td>$<%#= number_with_delimiter(movement.amount, delimiter: ".", separator: ",")</td> %>
              <%# <td><%#= movement.currency</td> %>
              <%# <td><%#= movement.description</td> %>
              <%# <td><%#= movement.type</td> %>
            <%# </tr> %>
            <%# end %>

            <tr>
              <td><%= movement['transaction_date'].strftime('%d-%m-%Y') %></td>
              <td><%= movement['description'] %></td>
              <td>$<%= number_with_delimiter(try_integer(movement['amount'].to_s.gsub(/[.]/, "")), delimiter: ".", separator: ",") %></td>
              <td><%= movement['currency'] %></td>
              <td><%= movement.recipient_account.holder_name %></td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
