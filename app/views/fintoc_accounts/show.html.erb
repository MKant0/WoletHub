<div class="container my-4">
  <div class="d-flex justify-content-center align-items-center">
    <%# <div class="ring">
      <h2 class="ring-text">$<%= number_with_delimiter(@fintoc_account.balance.available, delimiter: ".", separator: ",") %></h2>
    <div class="ring">
      <h2 class="ring-text">$<%= number_with_delimiter(@fintoc_access[0].balance.to_s.split(" ")[0], delimiter: ".", separator: ",") %></h2>
    </div>
    </div>
  </div>
    <%# <h2><%= @fintoc_access[0].balance.to_s.split(" ")[0] %><h2>
    <%# <%= console %>
  </div>

  <div class="d-flex justify-content-center align-items-center">
    <h3>Todas sus transacciones de la <%= @fintoc_account.name %> de <%= @bank_account.name %> </h3>
  </div>

  <div class="table-responsive small my-4">
    <table class="table table-striped table-sm">
      <thead>
        <tr>
          <th scope="col">Nombre</th>
          <th scope="col">Monto</th>
          <th scope="col">Moneda</th>
          <th scope="col">Descripción</th>
        </tr>
      </thead>
      <tbody>
        <%# <% @movements.each do |movement| %>
        <%= @fintoc_movement.each do |movement| %>
        <tr>
          <% if movement.sender_account %>
            <td><%= movement.sender_account.holder_name %></td>
          <% else %>
            <td>No holder name available</td>
          <% end %>
          <td>$<%= number_with_delimiter(movement.amount, delimiter: ".", separator: ",") %></td>
          <td><%= movement.currency %></td>
          <td><%= movement.description %></td>
          <td><%= movement.type %></td>
        </tr>
        <% end %>
          <%# <tr>
            <%# <td><%= movement['id'] %></td>
            <%# <td>$<%= number_with_delimiter(movement['amount'], delimiter: ".", separator: ",") %></td>
            <%# <td><%= movement['currency'] %></td>
            <%# <td><%= movement['description'] %></td>
          <%# </tr> %>
      </tbody>
    </table>
  </div>
</div>
